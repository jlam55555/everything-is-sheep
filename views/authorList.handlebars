<div id="postViews">
  {{#each authorList}}
    <a class="imgLink postView" href="/authors/{{filename}}">
      <img src="{{profilePicture}}" class="postViewAuthorPicture authorViewPicture">
      <h2>{{name}}</h2>
      <p>{{{formatDescriptionNoLinks}}}</p>
    </a>
  {{/each}}
</div>
